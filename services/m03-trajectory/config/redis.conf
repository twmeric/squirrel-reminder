# Redis生产环境配置

# 内存限制
maxmemory 256mb
maxmemory-policy allkeys-lru

# 持久化
appendonly yes
appendfsync everysec

# 性能优化
tcp-keepalive 300
timeout 0
tcp-backlog 511

# 日志
loglevel warning

# 安全
# requirepass ${REDIS_PASSWORD}

# 连接限制
maxclients 10000
